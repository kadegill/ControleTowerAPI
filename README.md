# ControleTowerAPI
A REST API built with Flask, pandas, and SQLite for querying OpenFlights aviation data. Supports CRUD on airports and airlines, pagination, filtering, and OpenAPI 3 docs. Includes matplotlib visualizations and geopy distance calculations.
